# Synthetic fixtures: no download required. Used by CPU smoke runs and CI.
subset: synthetic
n_reactions: 24
split_seed: 0
fractions: [0.6, 0.2, 0.2]
filter:
  min_barrier: 5.0
  min_rp_rmsd: 0.3
  monotonic_noise_frac: 0.05
  require_single_barrier: true
  max_removed_frac: 0.30
